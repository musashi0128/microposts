<%= form_for(current_user.favorite_microposts.find_by(micropost_id: @micropost.id),
            html: { method: :delete },
            remote: true) do |f| %>
 <%= f.submit "UnFavorite", class: "btn" %>
<% end %>